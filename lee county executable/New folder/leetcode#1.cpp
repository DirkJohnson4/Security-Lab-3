class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        
    }
    for (int i = 0; i < nums.len; i++); { 
        if num[i] + nums[i + 1] = target{
            std::cout << "[" << i << "," << i+1 << "]" << endl;
        }
    }
};